import { Builder } from '@builder.io/react';
import './TripleColumns/TripleColumns.builder';

Builder.register('insertMenu', {
  name: 'Simple components',
  items: [{ name: 'Triple Columnss' }],
});
